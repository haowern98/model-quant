{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions for local model file workflows.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
